# Node modules - do not push these
node_modules/
node_modules/.package-lock.json

# Environment variables (API keys, DB credentials)
.env
.env.test

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# VSCode settings
.vscode/

# OS-specific files
.DS_Store
Thumbs.db

# Build or compiled outputs (if used later)
dist/
build/

# Coverage (if testing later)
coverage/

# Ignore SQLite or other temp DBs (if ever used)
*.sqlite
*.db

# Ignore sensitive credential JSON files (if any)
*.secret.json
